# Database
DATABASE_URL="postgresql://user:password@localhost:5432/coworking_booking?schema=public"

# Node Environment
NODE_ENV="development"

# Email Configuration (Optional)
# Set DISABLE_EMAILS=true to completely disable email notifications
DISABLE_EMAILS="false"

# SMTP Settings (Required for production email sending)
# Leave empty to use console logging in development
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@coworking.com"

# Error Monitoring (Optional)
SENTRY_DSN=""
